MascData = [:]
MascData.new = { node, version ->
	def masc = [:]
	masc.text = Datasource {
		server node.server
		name "${node.id}:masc.text_${version}"
	}
	
	masc.json = Datasource {
		server node.server
		name "${node.id}:masc.gold_${version}"
	}
	masc.gold = masc.json
	return masc
}