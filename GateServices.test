include 'Servers'
include 'Gate'
include 'Test'

println "Getting the gate services."
GateServices.each { name, closure ->
	println name
}

def gate = GateServices.new(servers.localhost, '1.3.1-SNAPSHOT')

test("gate is null", gate != null)
println "gate is a ${gate}"

println "Defined services are:"
gate.each { name,service ->
	println "${name} -> ${service.endpoint}"
}
