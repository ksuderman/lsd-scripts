passed = 0
failed = 0

test = { condition, message=null ->
	if (!condition) {
		println message ?: 'Test failed.'
		++failed
	}
	else {
		++passed
	}
}

printTestResults = {
	println "Passed: ${passed}"
	println "Failed: ${failed}"
}